{
  "name": "blender-docs",
  "version": "1.0.0",
  "description": "The wiki for the Blender Modding API.",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Boofi",
  "license": "ISC",
  "devDependencies": {
    "vitepress": "^1.3.2"
  }
}